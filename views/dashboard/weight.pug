script(type='module').
   import * as weight from '/js/weight.js';

   const slot = document.getElementById('formContainer');

   const form = slot.appendChild(await weight.createForm());


h2 Weight
slot#formContainer

